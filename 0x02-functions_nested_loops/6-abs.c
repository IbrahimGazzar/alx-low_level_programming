#include "main.h"

/**
 * _abs - prints the absolute value of a given number
 * @n: input to be checked
 *
 * Return: the absolute value
 */

int _abs(int n)
{
	if (n < 0)
		return (0 - n)
	return (n)
}
